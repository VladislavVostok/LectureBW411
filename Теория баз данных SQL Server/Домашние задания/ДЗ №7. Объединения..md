
### Тема: Объединения.
___
#### Запросы:

1. Вывести названия аудиторий, в которых читает лекции преподаватель "Edward Hopper". 
2. Вывести фамилии ассистентов, читающих лекции в группу "F505". 
3. Вывести дисциплины, которые читает преподаватель "Alex Carmack" для групп 5-го курса. 
4. Вывести фамилии преподавателей, которые не читают лекции по понедельникам. 
5. Вывести названия аудиторий, с указанием их корпусов, в которых нет лекций в среду второй недели на третьей паре. 
6. Вывести полные имена преподавателей факультета "Computer Science", которые не курируют группы кафедры "Software Development". 
7. Вывести список номеров всех корпусов, которые имеются в таблицах факультетов, кафедр и аудиторий. 
8. Вывести полные имена преподавателей в следующем порядке: деканы факультетов, заведующие кафедрами, преподаватели, кураторы, ассистенты. 
9. Вывести дни недели (без повторений), в которые имеются занятия в аудиториях "A311" и "A104" корпуса 6.

___
#### Структура базы данных

![[7.1.png]]
___

### Описание

База данных **Академия** (Academy) содержит информацию о сотрудниках, внутреннем устройстве академии и читаемых лекциях. 

Преподаватели, читающие лекции в академии представлены в виде таблицы **Преподаватели** (Teachers), в которой собрана основная информация, такая как: имя и фамилия. Кроме этого есть возможность узнать является ли преподаватель ассистентом, куратором, деканом или заведующим при помощи соответствующих таблиц: **Ассистенты** (Assistants), **Кураторы** (Curators), **Деканы** (Deans) и Заведующие (**Heads**). 

Также в базе данных присутствует информация о группах, хранимая в таблице **Группы** (Groups). Данные о факультетах и кафедрах содержатся в таблицах **Факультеты** (Faculties) и **Кафедры** (Departments) соответственно. 

Помимо этого, база данных хранит информацию, связанную с проводимыми лекциями. Расписание лекций содержится в таблице **Лекции** (Lectures), а описание дисциплин, по которым читаются лекции, в таблице **Дисциплины** (Subjects). 

Информация о студентах содержится в таблице **Студенты** (Students).

___
### Таблицы

Ниже представлено детальное описание структуры каждой таблицы. 

**Ассистента (Assistants)** 
	1. **Идентификатор (Id).** Уникальный идентификатор ассистента. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Идентификатор преподавателя (TeacherId).** Преподаватель, который является ассистентом. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 

**Кураторы (Curators)** 
	1. **Идентификатор (Id).** Уникальный идентификатор куратора. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ.
	2. **Идентификатор преподавателя (TeacherId).** Преподаватель, который является куратором. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 

**Деканы (Deans)** 
	1. **Идентификатор (Id).** Уникальный идентификатор декана. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Идентификатор преподавателя (TeacherId)**. Преподаватель, который является деканом. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 

**Кафедры (Departments)** 
	1. **Идентификатор (Id).** Уникальный идентификатор кафедры. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Корпус (Building).** Номер корпуса, в котором располагается кафедра. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Должно быть в диапазоне от 1 до 5. 
	3. **Название (Name).** Название кафедры. 
		1. Тип данных - nvarchar(100).
		2. Не может содержать null-значения. 
		3. Не может быть пустым. 
		4. Должно быть уникальным. 
	4. **Идентификатор факультета (FacultyId).** Факультет, в состав которого входит кафедра. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 
	5. **Идентификатор заведующего (HeadId).** Заведующий кафедрой. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 

**Факультеты (Faculties)** 
	1. **Идентификатор (Id).** Уникальный идентификатор факультета. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Корпус (Building).** Номер корпуса, в котором располагается факультет. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Должно быть в диапазоне от 1 до 5. 
	3. **Название (Name).** Название факультета. 
		1. Тип данных - nvarchar(100).
		2. Не может содержать null-значения. 
		3. Не может быть пустым. 
		4. Должно быть уникальным. 
	4. **Идентификатор декана (DeanId)**. Декан факультета. 
		1. Тип данных - int.
		2. ▷ Не может содержать null-значения. 
		3. ▷ Внешний ключ. 

**Группы (Groups)** 
	1. **Идентификатор (Id)**. Уникальный идентификатор группы. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
		5. Тип данных - nvarchar(10).
		6. Не может содержать null-значения. 
		7. Не может быть пустым. 
		8. Должно быть уникальным. 
	3. **Курс (Year).** Курс (год) на котором обучается группа. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Должно быть в диапазоне от 1 до 5. 
	4. **Идентификатор кафедры (DepartmentId).** Кафедра, в состав которой входит группа. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 

**Группы и кураторы (GroupsCurators)** 
	1. **Идентификатор (Id).** Уникальный идентификатор группы и куратора. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Идентификатор куратора (CuratorId)**. Куратор. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 
	3. **Идентификатор группы (GroupId).** Группа. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 

**Группы и лекции (GroupsLectures)** 
	1. Идентификатор (Id). Уникальный идентификатор группы и лекции. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. Идентификатор группы (GroupId). Группа. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 
	3. **Идентификатор лекции (LectureId).** Лекция. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 

**Заведующие (Heads)** 
	1. **Идентификатор (Id).** Уникальный идентификатор заведующего. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Идентификатор преподавателя (TeacherId).** Преподаватель, который является заведующим. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 

**Аудитории (LectureRooms)** 
	1. **Идентификатор (Id).** Уникальный идентификатор аудитории. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Корпус (Building).** Номер корпуса, в котором располагается аудитория. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Должно быть в диапазоне от 1 до 5. 
	3. **Название (Name).** Название аудитории. 
		1. Тип данных - nvarchar(10).
		2. Не может содержать null-значения. 
		3. Не может быть пустым. 
		4. Должно быть уникальным. 

**Лекции (Lectures)** 
	1. **Идентификатор (Id).** Уникальный идентификатор лекции. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Идентификатор дисциплины (SubjectId).** Дисциплина, по которой читается лекция. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 
	3. **Идентификатор преподавателя (TeacherId).** Преподаватель, который читает лекцию. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 

**Расписания (Schedules)** 
	1. **Идентификатор (Id).** Уникальный идентификатор расписания. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Пара (Class).** Номер пары, на которой читается лекция. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Должно быть в диапазоне от 1 до 8. 
	3. **День недели (DayOfWeek).** День недели, в который читается лекция. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Должен быть в диапазоне от 1 до 7. 
	4. **Неделя (Week).** Номер недели, на которой читается лекция. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Должно быть в диапазоне от 1 до 52. 
	5. **Идентификатор лекции (LectureId).** Лекция. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 
	6. **Идентификатор аудитории (LectureRoomId).** Аудитория, в которой читается лекция. 
		1. Тип данных - int.
		2. Не может содержать null-значения. 
		3. Внешний ключ. 

**Дисциплины (Subjects)** 
	1. **Идентификатор (Id).** Уникальный идентификатор дисциплины. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Название (Name).** Название дисциплины. 
		1. Тип данных - nvarchar(100).
		2. Не может содержать null-значения. 
		3. Не может быть пустым. 
		4. Должно быть уникальным. 

**Преподаватели (Teachers)** 
	1. **Идентификатор (Id)**. Уникальный идентификатор преподавателя. 
		1. Тип данных - int.
		2. Авто приращение. 
		3. Не может содержать null-значения. 
		4. Первичный ключ. 
	2. **Имя (Name)**. Имя преподавателя. 
		1. Тип данных - nvarchar(max).
		2. Не может содержать null-значения. 
		3. Не может быть пустым. 
	3. **Фамилия (Surname).** Фамилия преподавателя. 
		1. Тип данных - nvarchar(max).
		2. Не может содержать null-значения. 
		3. Не может быть пустым
___
